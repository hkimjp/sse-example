(ns hkimjp.ring-reitit-sse-test
  (:require [clojure.test :refer [deftest is testing]]
            [hkimjp.ring-reitit-sse :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
